language: python

python:
  - "2.7"

install:
  - pip install -r requirements.txt
  - pip install coveralls

script:
  - nosetests ./core/tests --with-coverage --cover-package=core --cover-html --cover-erase -v

 # Coverage report
after_success:
  - coveralls