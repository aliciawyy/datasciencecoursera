language: python

python:
  - "2.7"

install:
  - pip install -U pip
  - pip install -r requirements.txt
  - pip install coveralls

script:
  - export PYTHONPATH=$PWD/aliciawyy/dmining:$PYTHONPATH
  - nosetests ./models/tests --with-coverage --cover-package=models --cover-html --cover-erase -v

 # Coverage report
after_success:
  - coveralls